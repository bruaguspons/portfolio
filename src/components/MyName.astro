---
import TextColor from "./TextColor.astro";

interface Props {
    className?: string;
}
const { className } = Astro.props;
---

<a href="home" class={`mx-10 font-bold ${className}`}>
    <span>Bruno</span>
    <TextColor>Pons</TextColor>
</a>
