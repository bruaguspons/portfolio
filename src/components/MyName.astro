---
import TextColor from "./TextColor.astro";

interface Props {
    className?: string;
}
const { className } = Astro.props;

const { currentLocale } = Astro;
---

<a href={`/${currentLocale}/home`} class={` font-bold ${className} min-w-max`}>
    <span>Bruno</span>
    <TextColor>Pons</TextColor>
</a>
