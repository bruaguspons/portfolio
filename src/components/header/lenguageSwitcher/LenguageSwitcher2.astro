<div
    class="w-full h-full relative flex items-center cursor-pointer group-focus:bg-red-400"
>
    <input
        type="checkbox"
        id="check1"
        class="toggle absolute left-[-9000px] top-[-9000px] group"
    />
</div>

<style>
    /* input[type="checkbox"].toggle + div.container::before {
        content: "";
        width: 2em;
        height: 1em;
        background-color: hsl(0, 80%, 90%);
        border-radius: 1em;
        margin-right: 0.25em;
        transition: background-color 200ms ease-in-out;
    }

    input[type="checkbox"].toggle + div.container::after {
        display: flex;
        justify-content: center;
        align-items: center;
        position: absolute;
        content: "\2715";
        font-size: 0.5em;
        height: 0.9em;
        left: 0.2em;
        width: 1.8em;
        height: 1.8em;
        background-color: hsl(0, 80%, 60%);
        color: white;
        border-radius: 1em;
        transition:
            background-color 200ms ease-in-out,
            transform 200ms ease-in-out;
    } */

    /* input[type="checkbox"].toggle:focus + div.container::before {
        outline: 1px solid black;
    }

    input[type="checkbox"].toggle:checked + div.container::before {
        background-color: hsl(100, 70%, 90%);
    }

    input[type="checkbox"].toggle:checked + div.container::after {
        content: "\2713";
        transform: translateX(100%);
        background-color: hsl(100, 70%, 60%);
    }

    input[type="checkbox"].toggle:disabled + div.container {
        color: #777;
    }

    input[type="checkbox"].toggle:disabled + div.container::before {
        background-color: #ccc;
    }

    input[type="checkbox"].toggle:disabled + div.container::after {
        background-color: #777;
    } */
</style>
